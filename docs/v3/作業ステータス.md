# v3.0.0 作業ステータス

v3.0.0実装作業の進捗と、作業を再開する際の手順をまとめる。
パート構成の全体像は [概要](index.md) を参照。

## 進捗サマリ

| パート | 状態 | 備考 |
| --- | --- | --- |
| A | 実装完了 | 5 ツール削除・Python 系 opt-in 化・依存 extras 化・サブコマンド必須化・プリセット 20250710 削除 |
| B | 実装完了 | archive.py・パイプライン連携・run_id 発行・自動クリーンアップ・`--no-archive` |
| C | 実装完了 | severity 正規化・rule_url 付与・retry_command 埋め込み・smart truncation・SARIF / GA 出力 |
| D | 実装完了 | `--fail-fast`・ファイル hash キャッシュ（textlint のみ）・`execute_command` の Popen 一本化 |
| E | 実装完了 | `list-runs` / `show-run` サブコマンド・`ArchiveStore.list_tools` 追加 |
| F | 未着手 | `pyfltr mcp` サブコマンド・MCP ツール群・stdio 隔離 |

## 完了済み作業の内訳

### パート A

- `pyfltr/config.py`: 5ツール削除、`PYTHON_COMMANDS`刷新、`DEFAULT_CONFIG`既定値をopt-in化。
  `_PRESET_BASE`刷新、`_REMOVED_PRESETS`導入、`_extract_removed_command`ヘルパー追加
- `pyfltr/command.py`: autoflake/isort/black向けの2段階実行分岐を削除
- `pyfltr/main.py`: argparse subparsers刷新、`_apply_subcommand_defaults` 追加、`_parse_subcommand` / `_build_effective_args` 削除
- `pyfltr/shell_completion.py`: サブパーサーを再帰的にwalkする実装に変更
- `pyproject.toml`: 依存整理、`[project.optional-dependencies].python`追加。
  `dev`グループに`"pyfltr[python]"`追加、`[tool.pyfltr].python = true`明示

### パート B

- `pyfltr/archive.py`: 新規追加
    - `default_cache_root` / `generate_run_id` / `ArchiveStore` / `ArchivePolicy` / `RunSummary` / `policy_from_config`
    - 書き込みAPI: `start_run` / `write_tool_result` / `finalize_run`
    - 読み取りAPI: `list_runs` / `read_meta` / `read_tool_meta` / `read_tool_output` / `read_tool_diagnostics`
    - クリーンアップ: `cleanup`（世代数・サイズ・期間の3軸）
- `pyfltr/config.py`: `archive` / `archive-max-runs` / `archive-max-size-mb` / `archive-max-age-days` の既定値追加
- `pyfltr/main.py`: `run_pipeline` でアーカイブ生成・クリーンアップ・書き込みフック組み立て・finalize実行
- `pyfltr/cli.py`: `run_commands_with_cli` / `render_results` に `archive_hook` / `run_id` 引数追加
- `pyfltr/ui.py`: `run_commands_with_ui` / `UIApp.__init__` に `archive_hook` 引数追加
- `pyfltr/llm_output.py`: `_build_header_record` / `write_jsonl_header` / `write_jsonl` / `build_lines` に `run_id` パラメーター追加

### テスト整備

- `tests/config_test.py`: プリセット20250710 / 削除ツールキーのValueErrorテスト追加、既定値の変更を反映、`python = true`で有効化されるテスト追加
- `tests/main_test.py`: サブコマンド必須化に合わせた修正、`_parse_subcommand` / `_build_effective_args` テスト削除
- `tests/shell_completion_test.py`: サブパーサー対応による動作確認
- `tests/executor_test.py`: 削除ツール参照をruff系へ置換
- `tests/cli_test.py` / `tests/command_test.py` / `tests/output_format_test.py`: 削除ツール参照をruff / mypyなどに置換
- `tests/archive_test.py`: 新規追加（書き込み・読み取り・クリーンアップ3軸を網羅）

### パート C

- `pyfltr/error_parser.py`: `_normalize_severity()`追加、各JSONパーサーで3値正規化。
  mypy / markdownlintの末尾ルール抽出、`_parse_with_pattern()`の名前付きグループ`rule`対応。
  `ErrorLocation.rule_url`追加、各カスタムパーサーで`rule_urls.build_rule_url()`呼出
- `pyfltr/rule_urls.py`: 新規追加
    - `build_rule_url(command, rule, *, existing_url=None, category=None)`
    - ツール別テンプレート関数を辞書ディスパッチ（ruff / pylint / pyright / mypy / shellcheck / eslint / markdownlint）
- `pyfltr/command.py`: `CommandResult.target_files` / `archived` / `retry_command`フィールド追加。
  `execute_command()`で`target_files`を全経路で設定
- `pyfltr/llm_output.py`: `rule_url`の条件付き出力、`retry_command` / `truncated`の`tool`レコード添付、
  `jsonl-*`設定キーによる切り詰め制御、`CommandResult.archived`による判定
- `pyfltr/config.py`: `jsonl-diagnostic-limit` / `jsonl-message-max-lines` / `jsonl-message-max-chars`追加
- `pyfltr/main.py`: `--output-format`にsarif / github-annotations追加。
  構造化出力の共通化として`_force_structured_stdout_mode()`・`_suppress_logging()`に改称・一般化。
  `_detect_launcher_prefix()` / `_build_retry_args_template()` / `_build_retry_command()`追加。
  `_archive_hook()`で書き込み成功時に`CommandResult.archived=True`
- `pyfltr/sarif_output.py`: 新規。SARIF 2.1.0互換dictを生成
- `pyfltr/github_annotations.py`: 新規。`::error file=...`形式の行群を生成
- `pyfltr/archive.py`: `_error_to_dict()`に`rule_url`追加

### テスト整備（パート C）

- `tests/error_parser_test.py`: severity正規化・mypy/markdownlintルール抽出・rule_url付与
- `tests/rule_urls_test.py`: 新規。ツール別URLテンプレートとtextlintのNone返却
- `tests/llm_output_test.py`: rule_url / retry_command / truncatedの条件付き出力と切り詰めテスト
- `tests/sarif_output_test.py`: 新規。SARIF 2.1.0構造整合（level変換・rules/results配置）
- `tests/github_annotations_test.py`: 新規。`::error` / `::warning` / `::notice`形式とfile/line出力
- `tests/config_test.py`: jsonl-*キーの既定値と不正値拒否
- `tests/main_test.py`: `--output-format`新choicesの受理と構造化出力時のstdout抑止・retry_commandテンプレート
- `tests/archive_test.py`: diagnostics.jsonlに`rule_url`が含まれる

### ドキュメント

- `docs/v3/index.md`: v3概要
- `docs/v3/依存整理と破壊的変更.md`: パートAの設計判断
- `docs/v3/実行アーカイブ.md`: パートBの設計判断
- `docs/v3/JSONL出力拡張.md`: パートCの設計判断
- `docs/v3/マイグレーションガイド.md`: 移行チェックリスト
- `docs/v3/作業ステータス.md`: 本ドキュメント
- `docs/guide/usage.md`: パートCで「出力形式」節・JSONLスキーマ節を更新。
  `rule_url` / `retry_command` / `truncated` の追記、sarif / github-annotationsの追加。
  残存していた`black`参照を`ruff-format`へ差し替え
- `docs/guide/configuration.md`: 削除ツール`pflake8`を参照していたfast除外例を`textlint`へ差し替え
- `docs/guide/configuration-tools.md`: `python = false で一括無効化`の表現をopt-in時代に合わせて`python = true で一括有効化`へ修正
- `docs/guide/recommended.md`: Pythonプロジェクト例に`python = true`と`pyfltr[python]`extras明示を追加
- `docs/guide/recommended-nonpython.md`: 冗長な`python = false`指定を除去し、opt-in化の前提説明に置き換え
- `README.md`: Python系opt-inの注意と`pyfltr[python]`extras案内を追加
- `CHANGELOG.md`: 新規作成。v3.0.0の破壊的変更・追加機能・変更点を整理
- `.claude/agents/tool-compat-checker.md`: descriptionから削除5ツール参照を除去し、現存ツール一覧へ更新
- `.claude/skills/pyfltr-add-tool/SKILL.md`: formatter例の`black`を`prettier`へ差し替え
- `mkdocs.yml`: navにv3セクション追加（JSONL出力拡張を含む）。
  llmstxt `markdown_description` にJSONL新フィールドとSARIF / GA出力を反映
- `CLAUDE.md`: JSONLスキーマ案内を新仕様（`run_id`・`rule_url`・`retry_command`・`truncated`）へ更新

### パート D

- `pyfltr/cache.py`: 新規追加
    - `CacheStore` / `CachePolicy` / `cache_policy_from_config()` / `is_cacheable()` / `resolve_config_files()`
    - `<cache_root>/cache/<tool>/<hash>.json` 構造の書き込み・読み取り・期間超過クリーンアップ
    - `--config` / `--ignore-path` 検出時のキャッシュ無効化
- `pyfltr/config.py`: `CommandInfo` に `cacheable` / `config_files` 追加。
  textlintのみ `cacheable=True` で設定ファイルを完全列挙し、`DEFAULT_CONFIG` に `cache` / `cache-max-age-hours` を追加
- `pyfltr/command.py`: `_run_subprocess()` をPopenベースに一本化、`_active_processes_lock` と `terminate_active_processes()` を追加。
  `CommandResult` に `cached` / `cached_from` を追加し、`execute_command()` にキャッシュ参照・書き込み経路を統合
- `pyfltr/cli.py` / `pyfltr/ui.py`: `--fail-fast` 中断経路を追加（`terminate()` → `kill()` フォールバック、`future.cancel()`、skipped CommandResult発行）。
  `cache_store` / `cache_run_id` を `execute_command` までスレッドスルーする
- `pyfltr/main.py`: `--fail-fast` / `--no-cache` フラグ追加。
  `run_pipeline` で `CacheStore` 生成・クリーンアップを行い、`cli.py` / `ui.py` へ受け渡す。キャッシュヒット結果はアーカイブ書き込みから除外する
- `pyfltr/llm_output.py`: toolレコードに `cached` / `cached_from` の条件付き出力を追加

### テスト整備（パート D）

- `tests/cache_test.py`: 新規（put/get・キー変化・期間クリーンアップ・`is_cacheable` / `resolve_config_files` / `cache_policy_from_config`）
- `tests/command_test.py`: `CommandResult.cached` / `cached_from` の既定値、execute_commandのキャッシュヒット / キャッシュ対象外テスト追加
- `tests/cli_test.py`: `--fail-fast` 中断時に後続がskipped化されるテスト、fail_fast=Falseでの通常進行テスト
- `tests/config_test.py`: `cache` / `cache-max-age-hours` の既定値・上書き・不正値拒否。
  `cacheable` / `config_files` がtextlintのみ完全列挙となり、その他ツールは `cacheable=False` 維持
- `tests/llm_output_test.py`: `cached` / `cached_from` の条件付き出力
- `tests/main_test.py`: `--fail-fast` / `--no-cache` フラグ受理、既存テストのsubprocessモックを `_run_subprocess` 直接モックへ移行
- `tests/output_format_test.py`: 既存テストのsubprocessモック移行

### ドキュメント（パート D）

- `docs/v3/パイプライン機能拡張.md`: Intake骨子を最終設計で差し替え
- `docs/v3/index.md`: 主な追加機能に `--fail-fast` / ファイルhashキャッシュ追記
- `docs/v3/作業ステータス.md`: 本内訳を追記、進捗表を更新
- `docs/guide/usage.md`: `--fail-fast` / `--no-cache` の説明とJSONL `cached` / `cached_from` の記載
- `docs/guide/configuration.md`: `cache` / `cache-max-age-hours` 設定キーの説明
- `CHANGELOG.md` / `CLAUDE.md` / `mkdocs.yml`: 新フラグ・新フィールドを反映

### パート E

- `pyfltr/runs.py`: 新規追加
    - `register_subparsers()`: `list-runs` / `show-run` のサブパーサー登録
    - `execute_list_runs()`: run一覧を新しい順でtext / json / jsonl出力
    - `execute_show_run()`: meta + ツール別サマリの既定表示、`--tool <name>` によるdiagnostics全件、`--tool <name> --output` による `output.log` 全文表示
    - `_resolve_run_id()`: 完全一致・前方一致・`latest` エイリアスを解決（前方一致で複数該当時はエラー）
    - `_stdout_owned`: json / jsonlモードでroot loggerを抑止しstdoutを専有する内部コンテキスト
- `pyfltr/archive.py`: `ArchiveStore.list_tools(run_id)` を追加。`tools/` 配下のサニタイズ済みツール名を自然順で返すSSOT
- `pyfltr/main.py`: `_ALL_SUBCOMMANDS` に2サブコマンドを追加。`build_parser()` での登録を `pyfltr.runs.register_subparsers()` へ委譲し、`run()` にディスパッチ分岐を追加

### テスト整備（パート E）

- `tests/runs_test.py`: 新規。`PYFLTR_CACHE_DIR` を `tmp_path` に固定するフィクスチャで `ArchiveStore` と `pyfltr.main.run([...])` の両経路が同一キャッシュを参照する状態を作る
    - `list-runs`: 空 / 複数 / `--limit` / json / jsonl
    - `show-run`: overview / prefix / latest / prefix ambiguous / 不在run_id / 不在tool
    - `--tool`: text / json / jsonl、`--output`: text / jsonl、`--output` without `--tool` のエラー系
- `tests/archive_test.py`: `list_tools` のテストを追加（空run / 複数ツール / 不在run_id）

### ドキュメント（パート E）

- `docs/v3/詳細参照サブコマンド.md`: Intake骨子に主要設計判断 / 却下した代替案 / 関連ファイル節を追加
- `docs/v3/作業ステータス.md`: 進捗表と本内訳を更新
- `docs/guide/usage.md`: `list-runs` / `show-run` のサブコマンド節追加、jsonlスキーマ節の「将来追加」表記を確定形へ更新
- `mkdocs.yml`: navへの `v3/詳細参照サブコマンド.md` 追加、llmstxt `markdown_description` にサブコマンド概要追記
- `CHANGELOG.md`: `[Unreleased] ### 追加` に2サブコマンドを追加
- `CLAUDE.md`: `header.run_id` から `pyfltr show-run` / `list-runs` への参照経路案内を追加

## 未完了作業の概要

### 残っているパート

- パートF: MCPサーバー化（`pyfltr mcp`サブコマンド・MCPツール群・`run_pipeline`の出力抑止モード）

各パートの詳細設計は本ディレクトリに個別ドキュメントとして追加する方針。

## 作業再開手順

1. 作業ブランチを最新化する（masterで作業中）
2. 残ドキュメント更新から着手する場合:
    - `README.md` / `docs/guide/*` / `mkdocs.yml` / `CLAUDE.md` / `CHANGELOG.md`を順に更新する
    - ドキュメント更新後に`uv run mkdocs build --strict`で整合性を確認する
3. 未完了パート（F）に着手する場合:
    - パート単位でタスクを分解し、`pyfltr/archive.py`と同じ粒度で新規モジュールを追加する
    - 各パート完了時に`uv run pyfltr run-for-agent`で検証する
4. すべての変更が完了したらバージョンを`3.0.0`としてリリースする

## 確定済みの設計判断（再確認不要）

以下はユーザー合意済みまたはエージェント判断で確定しており、v3.0.0実装全体で踏襲する。

- サブコマンドはargparse subparsers (`required=True`) で明示必須とする
- 実行アーカイブは既定有効、`--no-archive` または `archive = false` でオプトアウト可
- アーカイブ保存先はXDG準拠のユーザーキャッシュに集約（プロジェクトローカルには作らない）
- run_idはULID形式（26文字Crockford Base32）
- 自動クリーンアップは3軸（世代数 / サイズ / 期間）で古い順に削除
- Python系ツールは `pyfltr[python]` オプショナルグループに集約（細粒度extrasは不採用）
- MCPサーバーは本体同梱 (`pyfltr mcp` サブコマンド)、`mcp` / `python-ulid` / `platformdirs` は本体必須依存
- severity正規化は `error` / `warning` / `info` の3値、未対応ツールは `None` 省略
- rule_urlは既知ツール (ruff / pylint / pyright / mypy / shellcheck / eslint / markdownlint / textlint) のみ提供
- smart truncationはJSONL側でdiagnostic上限 + tool.message長を制限、truncate分はアーカイブへ全文保存
- ファイルhashキャッシュは既定有効 (`--no-cache` または `cache = false` でオプトアウト)、ファイル間依存を持たず設定もCWDのみで解決するlinter（textlint）のみ対象。既定保持時間12時間（`cache-max-age-hours`）、期間軸のみのシンプルなクリーンアップ
- `--fail-fast` 成立のため `execute_command` のsubprocess実行経路をPopenに一本化する
