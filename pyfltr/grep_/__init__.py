"""横断検索・置換のコアロジック。

`pyfltr grep`/`pyfltr replace`サブコマンドおよびMCPツールが共有する
パターンマッチング・ファイル走査・置換適用・undo世代管理のコア層を提供する。
サフィックス`_`は標準ライブラリ`re`との混同を避けるための命名で、
`pyfltr/cli/mcp_server.py`が`mcp`パッケージとの衝突を避ける方針と同じ流儀に揃える。

設計判断の概要は次の通り。

- マッチング実装は標準ライブラリ`re`に統一する（外部バイナリ依存を導入しない）
- replace履歴の保存先はXDG Base Directory準拠のユーザーキャッシュ配下とし、
  `pyfltr/state/archive.py`と同じ世代管理（ULID採番・3軸自動クリーンアップ）を採用する
- 履歴の保存内容は「変更前全文・変更後ハッシュ・各置換箇所の前後行」の3点で、
  変更後全文は変更前と差分から再構成可能なため二重保存しない
- CLI層・MCP層は`pyfltr/cli/`配下のモジュール群が担い、本サブパッケージは
  ロジックのみを公開する（CLIフレーム・JSONLレコード組み立てなどは別レイヤー）
"""
